import { Button } from "antd";

import { StyledHeader } from "./styled";

const Header = () => {
  return (
    <StyledHeader>
      <Button>Header</Button>
    </StyledHeader>
  );
};

export default Header;
